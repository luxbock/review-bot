__pycache__/
*.pyc

# Factory run scaffolding — ephemeral BRIEF/RESULT/etc., never shipped
/.factory/
# Claude Code session scaffolding — same story
/.claude/
# finder_ab.py's default --out, written into the checkout it is run from
/finder-ab.jsonl

# nix build's default out-link
/result
/result-*
